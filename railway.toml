[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "uv run uvicorn backend.main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/api/search?q=AAPL"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
